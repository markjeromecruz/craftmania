// Pure per-block visual flourishes drawn AFTER the base fill+stroke in
// inline drawBlock. Phase 2A populates this with the existing 5 details
// (GRASS, DIAMOND, SNOW, CACTUS, ICE) plus new biome block details.
//
// Signature: drawBlockDetail(ctx, x, y, block, BLOCK_SIZE) -> void
// Plain blocks (e.g., STONE, AIR, WOOD) are no-ops.

export function drawBlockDetail(_ctx, _x, _y, _block, _BLOCK_SIZE) {
  // Phase 0 stub. Phase 2A implements.
}
